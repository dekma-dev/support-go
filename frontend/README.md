# Frontend

## Local run

```powershell
cd frontend
copy .env.example .env
npm install
npm run dev
```

By default frontend calls `VITE_API_BASE_URL` (example: `http://localhost:8080`).

## Build

```powershell
cd frontend
npm run build
```
